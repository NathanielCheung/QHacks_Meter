# Optional: backend URL for parking sensor API (ESP32/Arduino).
# If set, the app will poll GET /api/parking every 5s for beamish-munro-hall and clergy-st-w.
# Run the server with: cd server && npm install && npm start
# VITE_PARKING_API_URL=http://localhost:3001

# Optional: Ollama for chatbot (disabled by default; chatbot uses rule-based answers unless enabled).
# When enabled, the app sends current parking data (streets, lots, availability, landmarks, user location)
# so Ollama can answer e.g. "How many spots on Princess?", "Is King Street full?", "Closest to me?", etc.
# Requires Ollama running locally (e.g. ollama run llama3.2).
# VITE_USE_OLLAMA=false
# VITE_OLLAMA_URL=http://localhost:11434
# VITE_OLLAMA_MODEL=llama3.2
